#!/bin/bash
# This script will be executed when the "Update" Rofi mode is selected.
# Add your update commands here.
echo "Running system update..."
# Example: sudo apt update && sudo apt upgrade -y
# Be careful with commands that require user interaction or sudo.
# You might want to use a graphical notification or a terminal for output.
